# Production smoke 403 diagnostic

This file is a temporary marker for diagnosing the production AI chat 403 response. The smoke workflow should capture the HTTP status and response body instead of letting curl hide the body behind `--fail`.
